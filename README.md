# Wallet Domain

Wallet entity for a payment system with error handling.

## Run Tests

```bash
go test -v ./...
```

## Run Server

```bash
go run .
```

Server starts on `:8080`. Health check at `GET /healthz`.
